let elasticSearch;_a3c‍.x([["search",()=>search]]);_a3c‍.w("../../../utils/search",[["elasticSearch",["elasticSearch"],function(v){elasticSearch=v}]]);

       const search = async ctx => {
  try {
    return elasticSearch(
      ctx,
      ['user', 'board', 'content', 'qna'],
      ['title', 'content', 'name', 'major'],
      'user/index',
      'api/search?search=' + ctx.query.search + '&page=',
    );
  } catch (e) {
    _a3c‍.g.console.log(e)
    ctx.throw(500, e);
  };
};