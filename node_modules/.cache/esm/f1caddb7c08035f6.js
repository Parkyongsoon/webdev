let bcrypt,db;_c08‍.x([["memberDeleteAuthIndex",()=>memberDeleteAuthIndex],["memberDeleteAuth",()=>memberDeleteAuth],["memberDeleteIndex",()=>memberDeleteIndex],["memberDelete",()=>memberDelete]]);_c08‍.w("bcryptjs",[["default",["bcrypt"],function(v){bcrypt=v}]]);_c08‍.w("../../../models",[["db",["db"],function(v){db=v}]]);



/**
 * myPageMemberDeleteCtrl
 */

       const memberDeleteAuthIndex = async ctx => {
  try {
    ctx.status = 200;

    await ctx.render('user/myPage/memberDelete/index', {
     errorMsg: ''
    });
  } catch (e) {
    ctx.throw(500, e);
  };
};

       const memberDeleteAuth = async ctx => {
 try {
   const { password } = ctx.request.body;

   const passwordAuth = await db.tbl_eluly_users.findOne({where: {id: ctx.params.id}});

   const isMatch = await bcrypt.compare(password, passwordAuth.password);

   if(!isMatch) {
     ctx.status = 200;

     await ctx.render('user/myPage/memberDelete/index', {
       errorMsg: '비밀번호가 틀렸습니다.'
     });
   } else {
     ctx.status = 200;

     return ctx.redirect('/api/myPage/memberDelete/' + ctx.params.id);
   };

 } catch (e) {
   ctx.throw(500, e);
 };
};

       const memberDeleteIndex = async ctx => {
 try {
   ctx.status = 200;

   await ctx.render('user/myPage/memberDelete/memberDelete');
 } catch (e) {
   ctx.throw(500, e);
 };
};

       const memberDelete = async ctx => {
 try {
   await db.tbl_eluly_users.destroy({ where: { id: ctx.params.id}, force: true});

   ctx.cookies.set('token', '');
   
   ctx.status = 200;

   return ctx.redirect('/');
 } catch (e) {
   ctx.throw(500, e);
 };
};