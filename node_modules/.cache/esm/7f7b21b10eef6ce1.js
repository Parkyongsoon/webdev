let fs;_0ee‍.x([["fileUnlink",()=>fileUnlink]]);_0ee‍.w("fs",[["default",["fs"],function(v){fs=v}]]);

       const fileUnlink = (length, path, dir, file) => {
  for(let i = 0; i < length; i++ ) {
    fs.unlink(path + dir + file[i], (err) => {
      if (err) throw err;
    });
  };
};